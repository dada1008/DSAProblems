package com.opensource.dada.problems.interviewbit.test;

/**
 * Problem:
 *         Count no. of odd fibonacci sum in given range of fibonacci numbers.
 *         like 2 to 6, there will be 3 odd fibonacci sum
 *         example -
 *         if x=2, then F(x)=1;
 *         if x=3, then F(x)=2;
 *         if x=4, then F(x)=3;
 *         if x=5, then F(x)=5;
 *         if x=6, then F(x)=8;
 *         so odd sum count is 3; i.e. for x = 2, 4,5
 */
public class CountOddFibonacciInRangeOfFibonacciSeries {
    public static void main(String[] args) {

    }
}
